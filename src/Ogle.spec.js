describe('Ogle', () => {
  beforeEach(() => {});
  afterEach(() => {});

  it('is an instance of `Ogle`', () => expect(ogle).toBeInstanceOf(Ogle));
  it('is defined', () => expect(ogle).toBeDefined());
});
