const debug = require("debug")("Ogle");

/** Class for visual diffing between domains */
class Ogle {
  /** Creates Ogle instance */
  constructor() {
    debug("constructor");
  }
}

module.exports = Ogle;
